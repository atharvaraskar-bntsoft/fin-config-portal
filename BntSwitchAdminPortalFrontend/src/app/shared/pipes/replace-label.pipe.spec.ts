import { ReplaceLabelPipe } from './replace-label.pipe';

xdescribe('ReplaceLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
